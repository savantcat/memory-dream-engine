# 记忆梦境引擎 — 部署指南

## 获取文件

```bash
git clone https://gitee.com/savantcat/memory-dream-engine.git
cd memory-dream-engine
```

```
memory-dream-engine/
├── engine.py                 # 核心引擎（~800行 Python，零三方依赖）
├── cli.py                    # 命令行入口 + 三个适配器示例
├── nightbrain_consolidate.py # 可选：夜间知识库巩固
├── SKILL.md                  # Hermes Agent 技能文件
├── README.md                 # 项目说明
├── 部署指南.md                # 本文件
└── tests/                    # 8 个单元测试
```

## 方式一：作为 Python 库使用

适用于任何支持 session_search + memory 工具的 AI Agent 框架。

### 1. 复制引擎文件

```bash
cp engine.py cli.py /your/agent/skills/memory-dream-engine/
```

### 2. 实现三个适配器函数

```python
from engine import DreamEngine

def my_session_search(query, limit, sort):
    """调用你的Agent框架的搜索接口"""
    # 示例：Hermes Agent
    # return session_search(query=query, limit=limit, sort=sort)
    pass

def my_memory(action, target, content, old_text=None):
    """调用你的Agent框架的记忆接口"""
    # 示例：Hermes Agent
    # return memory(action=action, target=target, content=content)
    pass

def my_get_usage():
    """返回 (已用字符数, 总字符数)"""
    return (1500, 2200)

engine = DreamEngine(
    session_search_fn=my_session_search,
    memory_fn=my_memory,
    get_usage_fn=my_get_usage,
)

report = engine.dream(days_back=2, existing_memories=[...])
print(report.summary())
```

### 3. 设置定时任务

```yaml
# Hermes Agent cron 配置
schedule: every 180m
skills: ["memory-dream-engine"]
prompt: 执行记忆梦境引擎五阶段流程。无新发现输出 [SILENT]。
```

## 方式二：作为 Hermes Agent Skill

如果你用的是 Hermes Agent（合尘猫同款），直接把 `SKILL.md` 放入技能目录即可：

```bash
# 技能目录
~\AppData\Local\hermes\skills\note-taking\memory-dream-engine\SKILL.md
```

Hermes Agent 会自动加载技能，使用内置的 `session_search` + `memory` 工具执行五阶段流水线。

## 五阶段流水线说明

| 阶段 | 功能 | 频率 |
|:-----|:-----|:-----|
| 采集 | 搜索最近对话中的决策/纠偏/工具/灵感信号 | 每次运行 |
| 评分 | 三维评分（持久50%+纠偏30%+复用20%），≥70分写入 | 每次运行 |
| 写入 | 去重检查 + 智能写入/替换 | 每次运行 |
| 优化 | 去重+过期+压缩（≥85%占用时） | 每3次触发 |
| 发芽 | 主题聚类，3条→星球帖，5条→公众号文章 | 每次运行 |

## 评分算法

- 持久价值：含「规则/止损/配置/API/工作流」等关键词 +20分/个
- 纠偏价值：含「不对/不要/禁止/改/换/停」等关键词 +25分/个
- 可复用：含「方法/模板/脚本/skill」等关键词 +25分/个
- 按信号类型动态调权：决策信号持久占70%，纠偏信号纠偏占70%

## 常见问题

**Q: 引擎能独立运行吗？**

不能。引擎只定义评分/去重/聚类逻辑，需要通过适配器连接 Agent 的工具才能读写数据。
（`cli.py` 用 mock 适配器演示完整流程，可直接跑；`python engine.py` 跑 v3 核心能力演示。）

**Q: 需要装什么依赖？**

核心引擎**零三方依赖**，纯标准库。只有跑单元测试需要 `pytest`：`pip install -r requirements.txt`。

**Q: 支持哪些 Agent 框架？**

任何支持 `session_search` + `memory` 两个工具的框架都可以。已适配 Hermes Agent。

**Q: 评分阈值可以调吗？**

可以。修改 `engine.py` 中 `SignalScorer.score()` 的阈值即可。

**Q: 多久运行一次？**

推荐每3小时。太频繁浪费token，太稀疏记忆断层。

**Q: 知识库路径在哪配？**

`nightbrain_consolidate.py`（可选的夜间巩固模块）通过环境变量 `MEMORY_KB_ROOT` 读取，
未设置时默认为 `~/memory-kb`。代码里不含任何个人路径。

## 作者

合尘猫 × 小甜甜（AI分身）
2026年6月

更多作品与实战：<https://savantcat.cn>
