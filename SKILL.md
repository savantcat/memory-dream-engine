---
name: memory-dream-engine
description: >-
  记忆梦境引擎 — 让你的AI拥有持续记忆、自动清理、内容发芽能力。
  v3.0：五阶段流水线 + 艾宾浩斯衰减 + 零LLM提取 + SQLite持久化。
  engine.py ~800行Python，8个单元测试，MIT开源。
version: 3.0
author: 合尘猫 × 小甜甜
license: MIT
tags:
  - memory
  - dreaming
  - knowledge-management
  - agent
  - second-brain
  - python
  - open-source
  - ebbinghaus
  - zero-llm
---

# 🌙 记忆梦境引擎 v3.0

> 让你的AI「记住过去、优化现在、产出未来」

## 开源仓库

Gitee: https://gitee.com/savantcat/memory-dream-engine

```
engine.py (~800行) — 核心引擎，v2流水线+v3新模块，8单元测试全绿
cli.py              — 命令行入口 + 适配器示例
SKILL.md            — 本文件，Hermes Agent 集成指南
部署指南.md          — 两种部署方式 + FAQ
tests/              — 8个单元测试
```

## 🆕 v3.0 新增（融合溯忆 Suyi 设计）

| 模块 | 功能 | 优势 |
|:-----|:-----|:-----|
| `EbbinghausDecay` | 艾宾浩斯遗忘曲线 | 30天半衰期，访问自动增强，低强度自动归档 |
| `ZeroLLMExtractor` | 零LLM事实提取 | 中英文正则匹配，**零token成本** |
| `MemoryStore` | SQLite持久化 | 双时序事实(valid_from/valid_to)，完整审计 |
| `TokenTracker` | Token节省追踪 | 每次运行量化节省量 |
| `DreamEngineV3` | 升级版引擎 | 一键 `ingest()` + `dream()` |

## 🆚 vs 溯忆(Suyi)独特优势

溯忆是被动存储 — 梦境引擎是主动生长：

| 特性 | 梦境引擎 | 溯忆 | 
|:-----|:------:|:----:|
| 艾宾浩斯衰减 | ✅ | ✅ |
| 零LLM提取 | ✅ 中英文 | ✅ 英文 |
| 五阶段流水线 | ✅ **独有** | ❌ |
| 信号采集(决策/纠偏) | ✅ **独有** | ❌ |
| 三维动态评分 | ✅ **独有** | ❌ |
| 内容发芽(sprout) | ✅ **独有** | ❌ |
| Token追踪 | ✅ **独有** | ❌ |

## v2 核心流水线（保留）

| 阶段 | 模块 | 功能 |
|:-----|:-----|:-----|
| 1. 采集 | `SignalCollector` | 四维信号（决策/纠偏/工具/灵感） |
| 2. 评分 | `SignalScorer` | 三维动态权重（按信号类型自适应） |
| 3. 写入 | `MemoryWriter` | 智能去重+合并+替换 |
| 4. 优化 | `MemoryOptimizer` | 深度清理（去重/过期/压缩，每3次触发） |
| 5. 发芽 | `SproutDetector` | 内容建议（3条→星球帖/5条→公众号） |

## 评分算法

- 决策信号：持久维度占70%权重
- 纠偏信号：纠偏维度占70%权重
- 工具信号：可复用维度占70%权重
- ≥90分→替换 | ≥70分→写入 | <70分→跳过

## Hermes Agent Cron 配置

```yaml
schedule: every 180m
skills: ["memory-dream-engine"]
prompt: 执行记忆梦境引擎五阶段流程。无新发现输出 [SILENT]。
```

## 🔗 与夜间巩固系统联动（v3.1 | 2026-06-06）

梦境引擎负责**短时记忆**（memory工具，事实/偏好/纠偏），夜间巩固负责**长时知识库**（.md文件，知识/分析/模板）。两者形成双循环：

```
梦境引擎 (180m)          夜间巩固 (2am daily)
├── 采集：四维信号         ├── 扫描：24h新知
├── 评分：三维权重         ├── 陈旧度：>30天标记
├── 写入：memory工具       ├── 交叉引用：孤立检测
├── 优化：去重过期         ├── 索引：自动更新
└── 发芽：内容建议         └── 报告：推送到飞书
         ↓                          ↓
   短时记忆 ←──互相滋养──→ 长时知识库
```

**交互式恢复流程**仍然需要手动执行（cron无法写memory），但夜间巩固报告会在每天凌晨推送，提示你哪些知识需要关注。

## ⚠️ 关键限制：Cron 环境无法写入 Memory

**这是记忆梦境引擎最大的运维陷阱。** Cron 子进程没有 `memory` 工具权限，导致阶段3（写入）和阶段4（优化）**每次运行都静默失败**。引擎会正常完成采集和评分，但≥70分的记忆条目会持续积压。

### 症状

- Cron 报告显示「阶段3：写入 — 受阻」「阶段4：优化 — 跳过」
- 同一批高价值记忆反复出现在多轮报告中（如 D1 84.5分连续3轮未写入）
- Memory 容量逐渐逼近上限但优化清理从未执行

### 恢复流程（交互式会话中手动执行）

1. **定位积压报告**：`session_search(query="记忆梦境引擎 写入 受阻", sort="newest")` 找最近1-2轮的输出
2. **提取待写入条目**：从引擎报告的「应写入的N条」中拿出标题和分数
3. **检查覆盖**：对比已有 memory，跳过已覆盖的条目（如「主动做事框架」已在 USER PROFILE）
4. **写入新条目**：只写入真正全新的高价值条目（如风控原则），避免重复
5. **执行优化**：压缩冗余条目、清除过期信息、合并近义内容
6. **示例**：2026-06-05 的 4 条积压中，仅风控原则(80.5分)是全新条目，其余3条已被现有 memory 覆盖

### 建议

- **每 3 轮 cron 后安排一次交互式会话**手动执行阶段3+4
- Cron 报告中的「建议合尘猫下次在交互式会话中手动执行」提示不应忽略
- Memory 达 95%+ 时必须立即执行阶段4清理

## 设计哲学

记忆不是越多越好，而是越「活」越好。
五阶段流水线 = AI的「睡眠周期」：浅睡(采集)→深睡(评分写入)→REM(发芽产出)。

## 适配

引擎核心不依赖任何特定Agent框架。通过 `session_search_fn`、`memory_fn`、`get_usage_fn` 三个函数接口连接任何系统。

## 作者

合尘猫 × 小甜甜（AI分身）
2026年6月 | v3.0

## License

MIT — 自由使用、修改、分发。
